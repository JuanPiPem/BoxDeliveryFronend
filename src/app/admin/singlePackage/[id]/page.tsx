import React from "react";
import SinglePackage from "components/admin/singlePackage/SinglePackage";

const packages = () => {
  return (
    <div>
      <SinglePackage />
    </div>
  );
};

export default packages;
